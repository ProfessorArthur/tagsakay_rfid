# Backend Workers - Environment Variables Template
# Copy this to .dev.vars for Cloudflare Workers local development
# NEVER commit .dev.vars - it contains your actual secrets!

# Neon Database Connection
# Get this from https://console.neon.tech → Your Project → Connection Details
# Format: postgresql://user:password@host.neon.tech/database?sslmode=require
DATABASE_URL=postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require

# JWT Authentication Secret
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Or: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-here-generate-a-new-one

# Session Cookie Secret (separate from JWT secret for defense-in-depth)
# Generate with the same command as JWT_SECRET
SESSION_SECRET=another-unique-secret-for-session-cookies

# Environment
NODE_ENV=development

